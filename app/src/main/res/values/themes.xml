<resources>
    <style name="Theme.IBOPlus" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- personalize aqui -->
    </style>

    <!-- Tema rápido de splash: só pinta o fundo com a imagem -->
    <style name="Theme.IBOPlus.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_bg</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>
</resources>
